apply plugin: 'com.android.application'

android {
    compileSdkVersion 25
    buildToolsVersion "25.0.1"
    defaultConfig {
        applicationId "com.example.chriskoeberle.githubexample"
        minSdkVersion 21
        targetSdkVersion 25
        versionCode 1
        versionName "1.0"
        testInstrumentationRunner "android.support.test.runner.AndroidJUnitRunner"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
}
task copyTestResources(type: Copy) {
    from("${projectDir}/src/main/assets")
    into "${projectDir}/src/test/resources"
}

android.applicationVariants.all { variant ->
    def name = variant.name
    project.tasks["prepare${name.capitalize()}Dependencies"].dependsOn(copyTestResources)
}


dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    androidTestCompile('com.android.support.test.espresso:espresso-core:2.2.2', {
        exclude group: 'com.android.support', module: 'support-annotations'
    })
    compile 'com.android.support:appcompat-v7:25.0.1'
    testCompile 'junit:junit:4.12'
    testCompile fileTree(include: ['*.jar'], dir: 'testlibs')
    testCompile 'org.mockito:mockito-core:1.10.19'
    compile 'io.reactivex:rxandroid:1+'
    compile 'io.reactivex:rxjava:1+'
    compile 'com.squareup.okhttp3:okhttp:3.0.1'
    compile 'com.google.code.gson:gson:2.5'
    compile 'joda-time:joda-time:2.7'
    compile 'com.jakewharton.timber:timber:4.4.0'
}
